1. Download composer
2. Enter into your project directory and  Run the command composer update
3. Run the command "php artisan key:generate"
4. Then run the command "php artisan migrate"
5. Run the command "php artisan serve"

